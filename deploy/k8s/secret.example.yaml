# Template. Copy to secret.yaml, fill values, and keep out of git.
# Use 'kubectl create secret generic ... --from-literal' for one-shots.
apiVersion: v1
kind: Secret
metadata:
  name: omniquery-secrets
  namespace: omniquery
type: Opaque
stringData:
  WEB_API_KEYS: "replace-me-comma-separated"
  PERSIST_DATABASE_URL: "postgresql+asyncpg://omniquery:CHANGE_ME@postgres.data.svc.cluster.local:5432/omniquery"
  LLM_OPENAI_API_KEY: ""
  LLM_ANTHROPIC_API_KEY: ""
